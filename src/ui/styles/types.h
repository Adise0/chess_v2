#pragma once

namespace Chess::Rendering::Types {
enum Display { Flex, None };
enum VecticalAlignment { Top, Center, Bottom };
enum HorizontalAlignment { Left, Center, Right };
enum Position { Relative, Absolute };

} // namespace Chess::Rendering::Types
